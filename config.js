// Supabase connection.
// Paste your project URL and the "anon" public key from Supabase > Project Settings > API.
// The anon key is designed to be public: Row Level Security (supabase/schema.sql) protects the data.
// NEVER put the "service_role" key here.
//
// Leave both empty to run in demo mode (data stays in this browser only).
export const CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
